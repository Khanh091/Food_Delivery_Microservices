# Active delivery boundary

The active delivery projection is restored from delivery-service after every
authenticated bootstrap. V1 supports `ASSIGNED` → `PICKED_UP` → `DELIVERED`;
maps and navigation remain intentionally out of scope.
