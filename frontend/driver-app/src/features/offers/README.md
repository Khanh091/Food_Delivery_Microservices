# Offers boundary

The current offer is loaded from delivery-service. Push notifications only
trigger an authoritative refresh; the offer response and `expiresAt` control
the UI countdown and accept/reject actions.
